define([
	'intern!object',
	'intern/chai!assert'
], function (registerSuite, assert) {
	registerSuite({
		name: '<%= name %>',

		setup: function () {
			// before all tests run
		},

		teardown: function () {
			// after all tests run
		}
	});
});
